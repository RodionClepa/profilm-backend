export interface GenreType {
  id: number;
  name: string;
  apiName: string;
}

export interface GenreApiResponse {
  id: number;
  name: string;
  api_name: string;
}

export interface Genre {
  id: number;
  name: string;
}